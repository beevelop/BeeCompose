# redis.conf for Sentry self-hosted

# No memory limit by default — set e.g. "maxmemory 15gb" if needed
maxmemory 0

# Evict least-recently-used volatile keys (keys with TTL set).
# Redis is used both as a cache and processing queue in self-hosted Sentry.
maxmemory-policy volatile-lru
